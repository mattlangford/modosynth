---
texture_path: "/Users/mlangford/Downloads/test.bmp"
blocks:
    - name: "vco"
      description: "Voltage Controlled Oscillator"
      foreground_start: [32, 64] # Empty part of the texture map
      background_start: [0, 0]
      px_dim: [32, 16]
      inputs: 1
      outputs: 1
    - name: "vco_mega"
      description: "Voltage Controlled Oscillator, but MEGA"
      foreground_start: [32, 64] # Empty part of the texture map
      background_start: [0, 16]
      px_dim: [32, 32]
      inputs: 2
      outputs: 5
    - name: "lpf"
      description: "Low Pass Filter"
      foreground_start: [32, 64] # Empty part of the texture map
      background_start: [0, 48]
      px_dim: [32, 16]
      inputs: 3
      outputs: 1
    - name: "knob"
      description: "Voltage Control Knob"
      foreground_start: [42, 4]
      background_start: [42, 11]
      px_dim: [5, 5]
      # foreground_start: [50, 30]
      # background_start: [0, 64]
      # px_dim: [31, 31]
      inputs: 0
      outputs: 1
